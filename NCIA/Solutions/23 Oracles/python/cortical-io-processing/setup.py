from setuptools import setup, find_packages

setup(
    name='cortical.io.processing',
    version='0.1',
    author='Frans',
    long_description=__doc__,
    zip_safe=False,
    packages=find_packages(),
    install_requires=['']
)
